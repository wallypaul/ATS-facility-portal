services:
  portal:
    build:
      context: .
      args:
        # Set to the real API root on the server, e.g. https://api.example.com
        VITE_API_BASE: ${VITE_API_BASE:-http://localhost:8000}
    ports:
      - "3003:80"
    restart: unless-stopped
